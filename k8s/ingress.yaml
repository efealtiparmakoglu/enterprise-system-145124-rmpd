apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: enterprise-system-145124-rmpd-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: enterprise-system-145124-rmpd.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: enterprise-system-145124-rmpd-service
            port:
              number: 80
